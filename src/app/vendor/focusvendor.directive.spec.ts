import { FocusvendorDirective } from './focusvendor.directive';

describe('FocusvendorDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusvendorDirective();
    expect(directive).toBeTruthy();
  });
});
